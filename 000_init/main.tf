terraform {
  required_providers {
    # proxmox = {
    #   source = "bpg/proxmox"
    # }
    # macaddress = {
    #   source = "ivoronin/macaddress"
    # }
  }
}

# provider "proxmox" {
#   insecure = true
#   endpoint = local.proxmox.api_url
#   username = local.proxmox.ssh_username
#   password = local.proxmox.ssh_password
# }
